---
alwaysApply: true
---
## 接口文档规范（混合方案）

### 文档存放位置
- 所有接口文档必须放在 `docs/api/` 目录下
- 按**子模块**分组：`docs/api/模块名/子模块名.md`
- 一个文件包含 **10+ 个相关接口**
- 示例结构：
  ```
  docs/api/
  ├── sales/
  │   ├── README.md      # 模块概览
  │   ├── ppt.md         # PPT相关接口（查询、创建、更新、删除）
  │   ├── video.md       # 视频相关接口
  │   └── report.md      # 报表相关接口
  ├── user/
  │   ├── README.md
  │   └── auth.md        # 认证相关（登录、注册、登出）
  └── README.md          # 总索引
  ```

### 文档命名规范
- **文件名**：小驼峰，代表子模块功能，如 `ppt.md`, `auth.md`, `order.md`
- **目录名**：小驼峰，代表业务模块，如 `sales/`, `user/`, `product/`
- **原则**：相关性强的接口放在同一文件，如增删改查放一起

### 接口文档标准格式

当用户要求生成接口文档时，必须按以下模板输出：

#### 文件头部（必须）
```markdown
# 子模块名称（如：PPT管理）

> 本文档包含 PPT 相关的所有接口，包括查询、创建、更新、删除等操作。

**模块路径**：`/api/sales/ppt`  
**负责人**：张三  
**最后更新**：2025-10-31

---
```

#### 单个接口格式
```markdown
## 1. 接口名称（如：获取PPT列表）

### 基本信息
- **接口路径**：`/api/xxx/xxx`
- **请求方式**：`GET` / `POST` / `PUT` / `DELETE` / `PATCH`
- **接口说明**：简要描述接口用途
- **开发状态**：🔲 待开发 / 🔄 开发中 / ✅ 已实现（待联调） / ✅ 已实现 / 🐛 有问题 / 🗑️ 已废弃
- **开发者**：开发人员姓名
- **更新时间**：YYYY-MM-DD
- **权限要求**：需要登录 / 需要管理员权限 / 无
- **⚠️ 更新策略**：（仅更新类接口需要）**全量替换** 或 **增量更新**

## 请求参数

### Query 参数（GET）
| 参数名 | 类型 | 必填 | 说明 | 示例 |
|--------|------|------|------|------|
| page | number | 否 | 页码，默认1 | 1 |
| pageSize | number | 否 | 每页数量，默认20 | 20 |

### Body 参数（POST/PUT）
| 参数名 | 类型 | 必填 | 说明 | 示例 |
|--------|------|------|------|------|
| name | string | 是 | 产品名称 | "一表通" |
| type | string | 否 | 产品类型 | "监管" |

### 请求示例
{
  "name": "一表通",
  "type": "监管",
  "tags": ["PPT", "视频"]
}## 响应数据

### 成功响应（200）
{
  "code": 0,
  "message": "success",
  "data": {
    "list": [
      {
        "id": 1,
        "name": "一表通",
        "createTime": "2025-10-31"
      }
    ],
    "total": 100
  }
}### 响应字段说明
| 字段名 | 类型 | 说明 |
|--------|------|------|
| code | number | 状态码，0表示成功 |
| message | string | 响应消息 |
| data | object | 业务数据 |
| data.list | array | 数据列表 |
| data.total | number | 总数 |

### 错误响应
| HTTP状态码 | code | message | 说明 |
|-----------|------|---------|------|
| 400 | 400 | 参数错误 | 请求参数格式错误 |
| 401 | 401 | 未授权 | token失效或未登录 |
| 403 | 403 | 无权限 | 没有访问权限 |
| 404 | 404 | 资源不存在 | 接口或数据不存在 |
| 500 | 500 | 服务器错误 | 服务器内部错误 |

## 业务逻辑说明
1. 描述接口的核心业务逻辑
2. 特殊处理情况
3. 数据校验规则
4. 权限要求
5. **⚠️ 如果是更新类接口，必须明确说明更新策略**：
   - **全量替换（PUT）**：后端完全替换原有数据，前端必须传递完整数据
   - **增量更新（PATCH）**：后端只更新指定字段，前端只需传递修改的字段

### ⚠️ 更新策略说明（仅更新类接口需要）

如果接口涉及数据更新，必须明确说明采用哪种更新策略：

#### 📋 全量替换（PUT）
```markdown
### ⚠️ 重要说明：全量替换

此接口采用**全量替换**策略：
- ✅ **必须传递完整的数据对象**
- ❌ **不支持增量更新**
- 💡 **原因**：简化后端逻辑，避免复杂的深度合并

**正确用法**：
```typescript
// 1. 先获取完整数据
const data = await getData(id)

// 2. 修改需要的部分
data.field = "新值"

// 3. 保存完整数据
await updateData(id, data)
```
```

#### ✏️ 增量更新（PATCH）
```markdown
### ✅ 重要说明：增量更新

此接口采用**增量更新**策略：
- ✅ **只更新指定的字段**
- ✅ **无需传递完整数据**
- 💡 **使用场景**：快速修改单个或少量字段

**正确用法**：
```typescript
// 只传递需要修改的字段
await updateName(id, { name: "新名称" })
```
```

#### 🔍 如何选择？

| 更新策略 | HTTP方法 | 使用场景 | 优点 | 缺点 |
|---------|----------|---------|------|------|
| **全量替换** | PUT | 内容编辑（如幻灯片） | 逻辑简单，无冲突 | 需要传递完整数据 |
| **增量更新** | PATCH | 元数据修改（如名称） | 高效，精准 | 实现复杂 |

## 前端对接说明
// services/xxxService.ts
import request from '@/utils/request'

export interface GetPptListParams {
  page?: number
  pageSize?: number
}

export interface PptItem {
  id: number
  name: string
  createTime: string
}

export interface GetPptListResponse {
  list: PptItem[]
  total: number
}

/**
 * 获取PPT列表
 * @param params 查询参数
 * @returns PPT列表数据
 */
export async function getPptList(params: GetPptListParams) {
  return request.get<GetPptListResponse>('/api/sales/ppt/list', { params })
}## 测试用例
### 正常场景
- 输入：`{ page: 1, pageSize: 10 }`
- 预期：返回10条数据，total为总数

### 异常场景
- 输入：`{ page: -1 }`
- 预期：返回400错误，提示参数错误

## 变更记录
| 版本 | 更新时间 | 更新人 | 更新内容 |
|------|----------|--------|----------|
| v1.0 | 2025-10-31 | 张三 | 初始版本 |
| v1.1 | 2025-11-01 | 李四 | 新增pageSize参数 |

## 备注
- 其他需要说明的信息
- 已知问题
- TODO 项### AI 生成接口文档指令

当用户要求"生成接口文档"时，AI 必须：

1. **询问必要信息**（如果用户未提供）：
   - 接口所属模块和子模块（如：sales/ppt）
   - 接口路径和请求方式
   - 接口用途
   - 是否有相关接口（是否需要放在同一文件）

2. **自动推断信息**：
   - 从 Service 文件推断参数类型
   - 从函数名推断接口用途
   - 识别相关接口（增删改查、同一业务）
   - 从代码注释提取说明

3. **智能分组**：
   - 如果是新增接口，判断是否与现有文件相关
   - 相关接口（如 CRUD、业务流程）放在同一文件
   - 一个文件包含 10+ 个接口（完整的业务子模块）
   - 超过 20 个接口考虑按功能进一步拆分

4. **生成完整文档**：
   - 包含文件头部（模块说明）
   - 按顺序编号接口（1. 2. 3.）
   - 严格按照模板格式
   - 自动生成 TypeScript 类型定义
   - 提供测试用例示例

5. **保存位置提示**：
   ```
   已生成接口文档，请保存到：
   docs/api/[模块名]/[子模块名].md
   
   如：docs/api/sales/ppt.md
   ```

6. **索引更新**：
   - 自动更新模块的 `README.md`
   - 自动更新总索引 `docs/api/README.md`

---

## 完整示例：ppt.md

```markdown
# PPT管理

> 本文档包含 PPT 相关的所有接口，包括查询、创建、更新、删除等操作。

**模块路径**：`/api/sales/ppt`  
**负责人**：张三  
**最后更新**：2025-10-31

---

## 1. 获取PPT列表

### 基本信息
- **接口路径**：`GET /api/sales/ppt/list`
- **接口说明**：分页查询PPT列表，支持按产品、日期筛选
- **权限要求**：需要登录

### 请求参数

| 参数名 | 类型 | 必填 | 说明 | 示例 |
|--------|------|------|------|------|
| page | number | 否 | 页码，默认1 | 1 |
| pageSize | number | 否 | 每页数量，默认20 | 20 |
| product | string | 否 | 产品名称 | "一表通" |

### 成功响应（200）
```json
{
  "code": 0,
  "message": "success",
  "data": {
    "list": [
      {
        "id": 1,
        "title": "一表通产品介绍",
        "product": "一表通",
        "createTime": "2025-10-31"
      }
    ],
    "total": 100
  }
}
```

### 前端对接
```typescript
// services/salesService.ts
export interface GetPptListParams {
  page?: number
  pageSize?: number
  product?: string
}

export async function getPptList(params: GetPptListParams) {
  return request.get('/api/sales/ppt/list', { params })
}
```

---

## 2. 创建PPT

### 基本信息
- **接口路径**：`POST /api/sales/ppt`
- **接口说明**：创建新的PPT记录
- **权限要求**：需要管理员权限

### 请求参数

| 参数名 | 类型 | 必填 | 说明 | 示例 |
|--------|------|------|------|------|
| title | string | 是 | PPT标题 | "一表通产品介绍" |
| product | string | 是 | 所属产品 | "一表通" |
| fileUrl | string | 是 | 文件URL | "https://..." |

### 请求示例
```json
{
  "title": "一表通产品介绍",
  "product": "一表通",
  "fileUrl": "https://cdn.example.com/ppt/xxx.pptx"
}
```

### 成功响应（200）
```json
{
  "code": 0,
  "message": "创建成功",
  "data": {
    "id": 101,
    "title": "一表通产品介绍",
    "createTime": "2025-10-31"
  }
}
```

### 前端对接
```typescript
export interface CreatePptParams {
  title: string
  product: string
  fileUrl: string
}

export async function createPpt(data: CreatePptParams) {
  return request.post('/api/sales/ppt', data)
}
```

---

## 3. 更新PPT

### 基本信息
- **接口路径**：`PUT /api/sales/ppt/:id`
- **接口说明**：更新指定PPT的信息
- **权限要求**：需要管理员权限

### 路径参数
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | number | 是 | PPT的ID |

### 请求参数
同"创建PPT"，所有字段可选

### 前端对接
```typescript
export async function updatePpt(id: number, data: Partial<CreatePptParams>) {
  return request.put(`/api/sales/ppt/${id}`, data)
}
```

---

## 4. 删除PPT

### 基本信息
- **接口路径**：`DELETE /api/sales/ppt/:id`
- **接口说明**：删除指定的PPT
- **权限要求**：需要管理员权限

### 路径参数
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | number | 是 | PPT的ID |

### 成功响应（200）
```json
{
  "code": 0,
  "message": "删除成功"
}
```

### 前端对接
```typescript
export async function deletePpt(id: number) {
  return request.delete(`/api/sales/ppt/${id}`)
}
```

---

## 变更记录

| 版本 | 更新时间 | 更新人 | 更新内容 |
|------|----------|--------|----------|
| v1.0 | 2025-10-31 | 张三 | 初始版本，包含增删改查 |
| v1.1 | 2025-11-01 | 李四 | 新增product筛选参数 |

## 备注
- 文件上传功能需先调用文件上传接口获取 fileUrl
- 删除操作为软删除，不会真正删除文件
```

---

## 模块索引示例（README.md）

每个模块目录下需要有 `README.md` 作为索引：

```markdown
# Sales 模块接口文档

## 概述
销售模块包含 PPT、视频、报表等相关接口。

## 接口列表

### PPT管理 ([ppt.md](./ppt.md))
- 获取PPT列表 - `GET /api/sales/ppt/list`
- 创建PPT - `POST /api/sales/ppt`
- 更新PPT - `PUT /api/sales/ppt/:id`
- 删除PPT - `DELETE /api/sales/ppt/:id`

### 视频管理 ([video.md](./video.md))
- 获取视频列表 - `GET /api/sales/video/list`
- 上传视频 - `POST /api/sales/video`

### 报表统计 ([report.md](./report.md))
- 获取统计数据 - `GET /api/sales/report/stats`

## 更新记录
- 2025-10-31：创建模块，添加PPT管理接口
```

---

## 接口变更管理

### 开发状态标注

在接口文档中使用以下标记表示开发状态，明确责任人和更新时机：

| 标记 | 含义 | 负责人 | 更新时机 | 说明 |
|------|------|--------|---------|------|
| 🔲 **待开发** | 需求已确认，待开发 | 前端/产品 | 需求评审通过后 | 接口设计已确定，等待后端开发 |
| 🔄 **开发中** | 正在开发 | 后端 | 开始编码时 | 后端正在实现接口逻辑 |
| ✅ **已实现（待联调）** | 开发完成，待测试 | 后端 | 单元测试通过后 | 后端开发完成，可以开始联调 |
| ✅ **已实现** | 联调通过，可用 | 后端 | 前后端联调通过后 | 接口稳定可用，可以上线 |
| 🐛 **有问题** | 发现BUG，需修复 | 后端 | 测试发现问题时 | 联调发现问题，需要修复 |
| 🗑️ **已废弃** | 不再使用 | 后端 | 接口下线时 | 接口已停止维护，需迁移 |

#### 状态流转流程

```
🔲 待开发
    ↓ （后端开始开发）
🔄 开发中
    ↓ （单元测试通过）
✅ 已实现（待联调）
    ↓ （通知前端联调）
前后端联调测试
    ↓
测试通过？
    ├─ 是 → ✅ 已实现（更新响应示例，通知上线）
    └─ 否 → 🐛 有问题（修复后重新测试）
```

#### 状态更新示例

**阶段 1：开发完成**
```markdown
### 基本信息
- **接口路径**：`POST /api/documents/create`
- **请求方式**：`POST`
- **开发状态**：✅ 已实现（待联调）
- **更新时间**：2025-12-28 10:00
- **开发者**：张三
```

**Git Commit**：
```bash
[API] Implement - 文档管理 - 完成创建文档接口

详细说明：
- 已实现接口逻辑
- 单元测试通过
- 待前端联调
```

**阶段 2：联调通过**
```markdown
### 基本信息
- **接口路径**：`POST /api/documents/create`
- **请求方式**：`POST`
- **开发状态**：✅ 已实现
- **更新时间**：2025-12-28 14:30
- **开发者**：张三
```

**同时更新**：
- 填写实际的响应示例（从真实返回数据复制）
- 更新变更记录
- 通知可以上线

**Git Commit**：
```bash
[API] Complete - 文档管理 - 创建文档接口联调通过 (v1.0)

详细说明：
- 前后端联调测试通过
- 更新实际响应示例
- 接口可用，可以上线
```

---

### 变更流程

#### 1. 后端修改接口 → 前端更新

**详细步骤**：

1. **后端修改接口**：修改接口实现代码
2. **立即更新文档**：
   - 修改对应的 `docs/api/xxx/xxx.md` 文件
   - 更新接口路径、参数、响应格式等
   - 在文档中标注 `⚠️ 重大变更` 或 `✨ 新增字段`
3. **增加版本号**：
   - 重大变更（Breaking Changes）：主版本号+1（如 v1.0 → v2.0）
   - 新增字段/接口：次版本号+1（如 v1.0 → v1.1）
   - 修复 BUG：修订号+1（如 v1.0.0 → v1.0.1）
4. **填写变更记录**：
   ```markdown
   ## 变更记录
   | 版本 | 更新时间 | 更新人 | 更新内容 | 影响范围 |
   |------|----------|--------|----------|---------|
   | v2.0 | 2025-12-28 | 张三 | ⚠️ 响应格式从 {code} 改为 {success} | 前端需修改所有调用 |
   | v1.1 | 2025-12-27 | 李四 | ✨ 新增 category 筛选参数 | 可选，不影响现有代码 |
   ```
5. **通知前端**：
   - 提交 Git Commit，消息格式：`[API] 更新接口文档 - 模块名 v2.0`
   - 在团队协作工具（钉钉/企业微信/Slack）发送通知
   - @ 前端负责人，说明变更内容和影响范围
6. **前端根据文档更新**：
   - 阅读变更记录，了解影响范围
   - 根据文档更新 Service 层代码
   - 更新 TypeScript 类型定义
   - 处理兼容性问题
7. **联调测试**：前后端联调验证


#### 2. 前端提需求 → 后端实现

**详细步骤**：

1. **前端提出需求**：
   - 创建接口文档草稿（在文档中标注 `🔲 待开发`）
   - 定义接口路径、参数、响应格式
   - 说明业务场景和用途
2. **后端评审**：
   - 评估技术可行性
   - 优化接口设计（RESTful 规范、性能考虑）
   - 确认响应格式和错误处理
3. **确认文档**：
   - 双方达成一致，文档定稿
   - 标注开发计划和上线时间
4. **后端开发**：
   - 按文档实现接口
   - 编写单元测试
5. **更新文档状态**：
   - 将 `🔲 待开发` 改为 `✅ 已实现`
   - 添加实际返回的示例
   - 填写变更记录
6. **前端对接**：
   - 根据文档编写 Service 代码
   - 联调测试

---

### 变更类型标注

在文档中使用以下标记标注变更类型：

| 标记 | 含义 | 示例 | 影响 |
|------|------|------|------|
| ⚠️ **Breaking Change** | 破坏性变更 | 响应格式改变、字段删除 | 前端必须修改 |
| ✨ **New** | 新增字段/接口 | 新增可选参数 | 不影响现有代码 |
| 🐛 **Fix** | 修复问题 | 修复字段类型错误 | 建议前端同步修复 |
| 📝 **Docs** | 仅文档更新 | 补充说明、修正错误 | 无影响 |
| ⚡ **Performance** | 性能优化 | 优化查询速度 | 无影响（可能更快） |
| 🗑️ **Deprecated** | 已废弃 | 标记为将来删除 | 前端需迁移到新接口 |

#### 示例：标注重大变更

```markdown
## 4. 更新文档

### ⚠️ 重大变更（v2.0）

此接口在 v2.0 中进行了重大调整：

| 变更项 | v1.x | v2.0 | 迁移指南 |
|-------|------|------|---------|
| 响应格式 | `{code, message, data}` | `{success, data, error}` | [查看迁移指南](#migration-v2) |
| 参数名 | `docId` | `documentId` | 统一命名规范 |

**兼容性**：v1.x 将在 2026-01-01 后停止支持

### 迁移指南 {#migration-v2}

**v1.x 代码**：
\`\`\`typescript
const resp = await updateDocument(docId, data)
if (resp.code === 0) {
  console.log(resp.data)
}
\`\`\`

**v2.0 代码**：
\`\`\`typescript
const resp = await updateDocument(documentId, data)
if (resp.success) {
  console.log(resp.data)
} else {
  console.error(resp.error)
}
\`\`\`
```

---

### 版本号规范

采用 **语义化版本号（Semantic Versioning）**：`v主版本.次版本.修订号`

#### 规则

- **主版本号**：不兼容的 API 修改（Breaking Changes）
  - 例：响应格式改变、必填参数改变、字段删除
  - 版本号：v1.0 → v2.0
  
- **次版本号**：向下兼容的功能性新增
  - 例：新增可选参数、新增响应字段、新增接口
  - 版本号：v1.0 → v1.1
  
- **修订号**：向下兼容的问题修复
  - 例：修复文档错误、修复 BUG、完善说明
  - 版本号：v1.0.0 → v1.0.1

#### 版本标注位置

1. **文件头部**：
```markdown
# 文档管理

> 本文档包含 PPT 文档相关的所有接口。

**模块路径**：`/api/documents`  
**当前版本**：v2.1.0  
**最后更新**：2025-12-28
```

2. **单个接口**（如果接口单独有版本）：
```markdown
## 4. 更新文档

### 基本信息
- **接口版本**：v2.0
- **接口路径**：`PUT /api/documents/{id}`
```

---

### 变更通知机制

#### 1. Git Commit 规范

```bash
# 格式：[API] 变更类型 - 模块名 - 简要说明 (版本号)

# 示例
[API] Breaking Change - 文档管理 - 修改响应格式 (v2.0)
[API] New - 文档管理 - 新增category筛选参数 (v1.1)
[API] Fix - 模板管理 - 修复文档错误 (v1.0.1)
```

#### 2. 通知模板

**钉钉/企业微信消息**：

```
📢 接口变更通知

模块：文档管理
版本：v2.0 → v2.1
更新人：@张三
更新时间：2025-12-28 14:30

⚠️ 重大变更：
- PUT /api/documents/{id} 响应格式改变

✨ 新增功能：
- GET /api/documents 新增 category 筛选参数

📄 文档链接：
docs/api/ppt/document.md

@前端负责人 请尽快更新代码并联调
预计影响：5个接口调用需要修改
```

#### 3. 文档内醒目提示

在文档顶部添加醒目的变更提示：

```markdown
# 文档管理

> ⚠️ **重要通知（2025-12-28）**：本模块在 v2.0 中进行了重大更新，响应格式从 `{code, message, data}` 改为 `{success, data, error}`。请前端务必阅读 [迁移指南](#migration-v2) 并更新代码。

> 本文档包含 PPT 文档相关的所有接口。
```

---

### 前端根据文档更新代码的步骤

#### Step 1: 查看变更记录

打开对应的接口文档（如 `docs/api/ppt/document.md`），查看 **变更记录** 章节：

```markdown
## 变更记录
| 版本 | 更新时间 | 更新人 | 更新内容 | 影响范围 |
|------|----------|--------|----------|---------|
| v2.0 | 2025-12-28 | 张三 | ⚠️ 响应格式改变 | 所有接口调用 |
```

#### Step 2: 识别变更类型

- ⚠️ **Breaking Change**：必须立即修改，否则代码会报错
- ✨ **New**：可选，建议使用新功能
- 🐛 **Fix**：建议同步修复

#### Step 3: 更新 Service 层

**根据文档的"前端对接"章节，更新 TypeScript 代码**：

```typescript
// 旧代码（v1.x）
export async function updateDocument(id: string, params: any): Promise<{
  code: number
  message: string
  data?: any
}> {
  return axios.put(`${SERVER_URL}/documents/${id}`, params)
}

// 新代码（v2.0）- 根据文档更新
export async function updateDocument(id: string, params: UpdateDocumentParams): Promise<{
  success: boolean
  data?: any
  error?: string
}> {
  return axios.put(`${SERVER_URL}/documents/${id}`, {
    documentData: params.documentData,
    autoSave: params.autoSave !== undefined ? params.autoSave : false,
  })
}
```

#### Step 4: 更新类型定义

```typescript
// 根据文档的 TypeScript 类型定义部分
export interface UpdateDocumentParams {
  documentData: DocumentData  // ⚠️ v2.0 要求传递完整数据
  autoSave?: boolean
}
```

#### Step 5: 更新调用代码

搜索项目中所有调用该接口的地方，更新调用方式：

```typescript
// 旧调用
const resp = await updateDocument(id, data)
if (resp.code === 0) {
  console.log('成功')
}

// 新调用
const resp = await updateDocument(id, { documentData: data })
if (resp.success) {
  console.log('成功')
} else {
  console.error(resp.error)
}
```

#### Step 6: 运行测试

```bash
# 运行单元测试
npm run test

# 启动开发服务器，手动测试
npm run dev
```

---

### 后端根据文档实现接口的步骤

#### Step 1: 阅读接口文档

打开对应的接口文档，仔细阅读：
- 接口路径和请求方式
- 请求参数（必填/可选）
- 响应格式
- 业务逻辑说明
- 错误处理

#### Step 2: 创建数据模型

根据文档的参数和响应定义，创建后端模型：

```java
// Java 示例 - 根据文档创建 DTO
public class UpdateDocumentRequest {
    @NotNull(message = "documentData 不能为空")
    private DocumentData documentData;
    
    private Boolean autoSave = false;
    
    // getters and setters
}

public class UpdateDocumentResponse {
    private Boolean success;
    private Object data;
    private String error;
    
    // getters and setters
}
```

#### Step 3: 实现接口逻辑

```java
@RestController
@RequestMapping("/api/documents")
public class DocumentController {
    
    @PutMapping("/{id}")
    public UpdateDocumentResponse updateDocument(
        @PathVariable String id,
        @RequestBody UpdateDocumentRequest request
    ) {
        try {
            // 根据文档的业务逻辑实现
            // 1. 验证参数
            // 2. 全量替换文档数据
            // 3. 处理 autoSave 逻辑
            
            return UpdateDocumentResponse.builder()
                .success(true)
                .data(Map.of("updatedAt", LocalDateTime.now()))
                .build();
                
        } catch (Exception e) {
            return UpdateDocumentResponse.builder()
                .success(false)
                .error(e.getMessage())
                .build();
        }
    }
}
```

#### Step 4: 编写单元测试

```java
@Test
public void testUpdateDocument() {
    // 根据文档的"测试用例"章节编写测试
    UpdateDocumentRequest request = new UpdateDocumentRequest();
    request.setDocumentData(mockData);
    
    UpdateDocumentResponse response = controller.updateDocument("doc_123", request);
    
    assertTrue(response.getSuccess());
    assertNotNull(response.getData());
}
```

#### Step 5: 更新文档状态

接口开发需要分两个阶段更新文档状态：

**阶段 1：开发完成（单元测试通过）**

更新文档：
- 将 `🔲 待开发` 或 `🔄 开发中` 改为 `✅ 已实现（待联调）`
- 更新开发者和时间
- 提交 Git Commit

通知前端：
```
📢 接口可以联调了

接口：POST /api/documents/create
状态：✅ 已实现（待联调）
文档：docs/api/ppt/document.md

@前端负责人 请开始联调测试
```

**阶段 2：联调通过**

更新文档：
- 将 `✅ 已实现（待联调）` 改为 `✅ 已实现`
- 填写**实际的响应示例**（从真实返回数据复制，不要编造）
- 更新变更记录
- 提交 Git Commit

通知上线：
```
✅ 接口联调通过

接口：POST /api/documents/create
版本：v1.0
文档：docs/api/ppt/document.md

已完成前后端联调，可以上线
```

---

### 接口文档 Review Checklist

在更新接口文档前，请确认以下事项：

#### 文档完整性
- [ ] 接口路径、请求方式、说明是否完整？
- [ ] 请求参数（必填/可选、类型、说明）是否明确？
- [ ] 响应格式是否包含完整示例？
- [ ] 错误响应是否列出常见错误？
- [ ] 业务逻辑是否清晰？
- [ ] 前端对接代码是否可直接使用？

#### 变更管理
- [ ] 版本号是否正确增加？
- [ ] 变更记录是否填写完整？
- [ ] 重大变更是否有醒目标注？
- [ ] 是否提供迁移指南？
- [ ] 是否通知相关人员？

#### 代码示例
- [ ] TypeScript 类型定义是否正确？
- [ ] Service 函数示例是否可运行？
- [ ] 是否包含正确用法和错误用法对比？

---

### 接口废弃流程

当需要废弃旧接口时，按以下流程进行：

#### 阶段 1：标记为废弃（提前 1-2 个月）

在文档中添加废弃警告：

```markdown
## 4. 更新文档（已废弃）

> 🗑️ **废弃警告**：此接口将在 2026-03-01 停止支持，请迁移到新接口 `PUT /api/documents/v2/{id}`

### 废弃原因
- 响应格式不符合新规范
- 性能较差

### 迁移指南
请使用新接口：[更新文档 v2](#update-document-v2)

新接口改进：
- 更快的响应速度
- 统一的响应格式
- 支持增量更新
```

#### 阶段 2：双接口并行（1-2 个月）

- 旧接口继续维护，但不再新增功能
- 新接口正式上线
- 前端逐步迁移到新接口

#### 阶段 3：停止旧接口

- 旧接口返回 410 Gone 状态码
- 响应中包含迁移提示

```json
{
  "success": false,
  "error": "此接口已停止服务，请使用新接口 PUT /api/documents/v2/{id}"
}
```

---

### 协作最佳实践

#### 1. 文档先行
- 接口设计阶段就编写文档草稿
- 前后端共同 Review
- 达成一致后再开发

#### 2. 及时同步
- 接口修改后立即更新文档
- 通过 Git Commit 和协作工具通知
- 不要等到上线前才更新文档

#### 3. 版本管理
- 严格遵循语义化版本号规范
- 重大变更必须提前通知
- 提供充足的迁移时间

#### 4. 定期 Review
- 每周/每两周进行接口文档 Review
- 检查文档和代码是否一致
- 清理过时的文档

#### 5. 自动化工具
- 使用 Swagger/OpenAPI 自动生成文档
- 使用 TypeScript 生成类型定义
- 使用 Git Hooks 检查文档更新

---

### 快速参考

#### 开发状态速查表

| 状态 | 负责人 | 更新时机 | Git Commit 格式 | 通知内容 |
|------|--------|---------|----------------|---------|
| 🔲 待开发 | 前端/产品 | 需求确认后 | `[API] Plan - 模块名 - 接口说明` | 需求已确认，待排期 |
| 🔄 开发中 | 后端 | 开始编码 | `[API] Dev - 模块名 - 开始开发接口` | 正在开发 |
| ✅ 已实现（待联调） | 后端 | 单测通过 | `[API] Implement - 模块名 - 完成接口实现` | 可以开始联调 |
| ✅ 已实现 | 后端 | 联调通过 | `[API] Complete - 模块名 - 接口联调通过 (v1.0)` | 可以上线 |
| 🐛 有问题 | 后端 | 发现BUG | `[API] Bug - 模块名 - 修复问题描述` | 发现问题，正在修复 |
| 🗑️ 已废弃 | 后端 | 接口下线 | `[API] Deprecated - 模块名 - 废弃接口` | 请迁移到新接口 |

#### 变更类型速查表

| 变更 | 版本号变化 | 标记 | 通知方式 | 迁移时间 |
|------|-----------|------|---------|---------|
| 响应格式改变 | v1.0 → v2.0 | ⚠️ Breaking | Git + 协作工具 | 提前1-2月 |
| 删除字段 | v1.0 → v2.0 | ⚠️ Breaking | Git + 协作工具 | 提前1-2月 |
| 新增可选参数 | v1.0 → v1.1 | ✨ New | Git Commit | 无需迁移 |
| 新增响应字段 | v1.0 → v1.1 | ✨ New | Git Commit | 无需迁移 |
| 修复BUG | v1.0.0 → v1.0.1 | 🐛 Fix | Git Commit | 建议同步 |
| 文档补充 | 版本不变 | 📝 Docs | Git Commit | 无影响 |

#### Git Commit 模板

```bash
# 开发状态更新
[API] Plan - 文档管理 - 新增批量删除接口

详细说明：
- 接口设计已确认
- 文档：docs/api/ppt/document.md
- 计划开发时间：2025-12-30

[API] Implement - 文档管理 - 完成创建文档接口

详细说明：
- 已实现接口逻辑
- 单元测试通过
- 待前端联调
- 文档：docs/api/ppt/document.md

[API] Complete - 文档管理 - 创建文档接口联调通过 (v1.0)

详细说明：
- 前后端联调测试通过
- 更新实际响应示例
- 接口可用，可以上线

# Breaking Change
[API] Breaking Change - 文档管理 - 修改响应格式 (v2.0)

详细说明：
- 响应格式从 {code, message, data} 改为 {success, data, error}
- 影响所有接口调用
- 提供迁移指南：docs/api/ppt/document.md#migration-v2

# 新增功能
[API] New - 文档管理 - 新增category筛选参数 (v1.1)

详细说明：
- GET /api/documents 新增可选参数 category
- 不影响现有代码
- 文档：docs/api/ppt/document.md

# 修复问题
[API] Fix - 模板管理 - 修复字段类型错误 (v1.0.1)

详细说明：
- 修正文档中 slideCount 字段类型为 number
- 建议前端同步更新类型定义
```
